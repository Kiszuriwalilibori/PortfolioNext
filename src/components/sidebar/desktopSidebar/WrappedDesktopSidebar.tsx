import DesktopSidebar from ".";
import { Wrapper } from "./wrapper";

export const WrappedDesktopSidebar = () => {
    return (
        <Wrapper>
            <DesktopSidebar />
        </Wrapper>
    );
};
